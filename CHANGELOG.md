# 更新日志

## 2026-08-28 — 完善短链接创建校验与错误处理

### 已完成

- [x] 给创建短链接的 Service 增加 URL 和过期时间校验。
- [x] 完善 Handler 的 400、409、500 错误映射。

### 具体变更

- **URL 校验**：去掉原始 URL 首尾空白，使用 `net/url` 解析；拒绝空值、解析失败、非 HTTP/HTTPS 协议及缺少主机名的地址。这里检查的是地址格式，不检查目标网站是否在线。
- **过期时间校验**：仅保留 `ExpireAt`；未设置或为 `nil` 时表示永久有效，设置时必须晚于当前时间。参数检查在访问数据库前完成。
- **区分两类时间错误**：创建时传入非法时间返回 `ErrInvalidExpireTime`，由 Handler 映射为 400；访问已过期链接仍返回 `ErrShortLinkExpired`，映射为 410。
- **短码规则统一**：自定义短码限制为 1～8 位英文字母或数字，与请求结构体和数据库字段长度一致；未指定时仍生成 6 位随机短码。
- **错误响应修正**：非法 URL、过期时间、短码参数返回 400；自定义短码已占用返回 409；未知内部故障返回 500，详细错误只写服务器日志。
- **并发冲突处理**：Repository 识别 MySQL 1062 唯一键冲突，并兼容 GORM 的 `ErrDuplicatedKey`；即使预查询时短码不存在，插入时发生自定义短码冲突也能返回 409。
- **创建成功响应**：实际 HTTP 状态码由 200 修正为 201，与响应体的 `code` 保持一致。

### 验证记录

- 新增 `internal/shortlink/create_test.go`，使用 `go-sqlmock` 模拟数据库。
- 覆盖非法参数、永久有效、未来时间、正常创建、自定义短码重复、插入时发生冲突、数据库故障，以及已过期链接返回 410 的回归场景。
- `go test ./... -count=1`：通过。
- `go vet ./...`：通过。
- 上述验证使用模拟数据库，未修改真实数据库；真实 MySQL 接口联调尚未完成。

### 本次未包含

- 自动生成的随机短码发生碰撞后的重试机制。
- Worker Pool、消息队列等异步处理能力。

说明：上述两项开发任务已经完成；后续阅读代码、补充测试和手工联调属于巩固与验证，不再列为待实现功能。
